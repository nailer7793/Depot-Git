1) J'ai crée un projet console C# que j'ai ajouter a mon depot git
2) - git add -all
   - git commit -m "partiel"
   - git pull --rebase origin master
   - git push origin master
3) 
   - git checkout -b "dev"
   - git add HelloWolrd.txt
   - git commit -m "3emeQuestion"
   - git push -u origin "dev"
